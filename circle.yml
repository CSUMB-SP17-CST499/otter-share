machine:
    node:
      version: 7.2.0

dependencies:
    override:
        - chmod +x gradlew
        - ./gradlew dependencies
deployment:
  staging:
    branch: master
    heroku:
      appname: young-plains-98404
