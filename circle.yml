machine:
    environment:

dependencies:
    override:
        - chmod +x gradlew
        - ./gradlew dependencies
deployment:
  staging:
    branch: master
    heroku:
      appname: young-plains-98404
